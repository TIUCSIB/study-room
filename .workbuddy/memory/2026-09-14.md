# 2026-09-14

## 交接文件核对(lofi 自习室)

对 `%TEMP%\lofi-study-room-handoff.md` 做了一次全量核对:逐组件读源码 + 实跑 `pnpm build`(35s,`Build complete!`,零报错)+ 依赖树与目录实测。报告落在 `docs/handoff-review-2026-09-14.md`。

要点(供后续会话直接复用,避免重复核对):

- **架构描述可信**:Nuxt 4.5.2 实装、SSE + POST、pixel 图标集唯一、`VideoBackdrop` 三级回退 + `fetch(HEAD)` content-type 探测、聊天 200 条上限、全部文件 ≤300 行 —— 均与代码一致。
- **交接文件已过期处**:①Pinia 不在技术栈(package.json 无依赖、代码零 `defineStore`,`pnpm-lock` 里的 pinia 只是 nuxt 未启用的可选 peer);②`TodoPanel.vue` 已实现完整增删改 + localStorage,不是空壳;③`PomodoroPanel.vue` 已按 design.md §4.3 完整实现(5/25/50 预设、Pause/Resume/End、+5:00、卸载 keepalive 补录);④交接文件 §6 自称的 `src/domain` / vitest 不存在。
- **文档与代码不一致(重要)**:`server/utils/store.ts` 用 `join(process.cwd(), '.data')`,数据实际在**项目根** `.data/pomodoros.json`;而 AGENTS.md §2/§7 与 design.md §5 都写 `server/.data/`,三处文档均需更正。
- **`ws@8.18.0` + `@types/ws` 是死依赖**,与「已放弃 WebSocket」矛盾,应移除。
- **最大工程风险:项目没有 git 仓库**(无 `.git`),而交接文件明确提醒「用户也在改代码、别覆盖」——无回滚兜底。建议第一件事 `git init` + 基线提交。
- **规范待补**:`PanelKey` 类型只在 `ControlDock.vue` 的 `<script lang="ts">` 导出,`app.vue:59/61` 裸用未 import,`.nuxt` 生成的 d.ts 里也没有该类型(因未开 `typeCheck` 才不报错);`app.vue` 带 `<style>` 块违反 AGENTS.md §5.4;AGENTS.md §5.3 的 `@/` 别名规则未覆盖 `server/`。
- **未接线能力**:`store.ts` 的 `statsFor()` 已实现 today/streak/total/roomTodayCount 五个指标,但前端零消费方——做专注报告时可直接用。
- **design.md 自身脱节**:§2/§4.8 仍把 ControlDock 描述为 `[ 方括号 ]` 文本按钮(实际是 pixel 图标按钮);§4.6「开机引导(BootGate)」是空标题。
